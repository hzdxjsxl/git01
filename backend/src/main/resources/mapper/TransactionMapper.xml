<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.monitor.mapper.TransactionMapper">
    
    <resultMap id="BaseResultMap" type="com.monitor.entity.Transaction">
        <id column="id" property="id"/>
        <result column="transaction_no" property="transactionNo"/>
        <result column="amount" property="amount"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="merchant_id" property="merchantId"/>
        <result column="order_no" property="orderNo"/>
    </resultMap>
    
    <select id="selectByCursor" resultMap="BaseResultMap">
        SELECT id, transaction_no, amount, status, create_time, merchant_id, order_no
        FROM transaction
        <if test="cursor != null">
            WHERE id &lt; #{cursor}
        </if>
        ORDER BY id DESC
        LIMIT #{size}
    </select>
    
    <select id="selectMaxId" resultType="java.lang.Long">
        SELECT IFNULL(MAX(id), 0) FROM transaction
    </select>
    
</mapper>
